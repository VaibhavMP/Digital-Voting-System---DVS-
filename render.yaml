services:
  - type: web
    name: smart-voting
    runtime: python
    buildCommand: pip install -r requirments.txt
    startCommand: gunicorn app:app --workers 2 --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: FLASK_DEBUG
        value: false
